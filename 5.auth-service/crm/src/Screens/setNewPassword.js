import React from 'react'
import Auth from '../Components/authentication'
const setNewPassword = () => {

    return (
        <Auth initMode={"Change Password"} />
    )
}

export default setNewPassword