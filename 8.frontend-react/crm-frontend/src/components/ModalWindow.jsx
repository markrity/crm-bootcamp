import React from 'react';
import Modal from 'react-modal';

function ModelWindow(props) {

    return (
        <Modal isOpen={true} contentLabel='Add User'>
            {/* <Form/> */}
            Add user
        </Modal >
    );
    
}

export default ModelWindow;